---
phase: 03-battle-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/battle_controller_raid_ally.c
  - include/battle_controllers.h
autonomous: true

must_haves:
  truths:
    - "SetControllerToRaidAlly(battler) assigns gBattlerControllerFuncs[battler] and gBattlerControllerEndFuncs[battler]"
    - "CONTROLLER_OPENBAG in sRaidAllyBufferCommands maps to BtlController_Empty (no bag access)"
    - "CONTROLLER_CHOOSEACTION in sRaidAllyBufferCommands maps to AI-driven selection (same as PlayerPartner)"
    - "SetControllerToRaidAlly is declared in include/battle_controllers.h and callable from src/battle_controllers.c"
  artifacts:
    - path: "src/battle_controller_raid_ally.c"
      provides: "Full RaidAlly battle controller cloned from battle_controller_player_partner.c"
      exports: ["SetControllerToRaidAlly"]
    - path: "include/battle_controllers.h"
      provides: "SetControllerToRaidAlly function declaration"
      contains: "SetControllerToRaidAlly"
  key_links:
    - from: "SetControllerToRaidAlly"
      to: "gBattlerControllerFuncs[battler]"
      via: "direct assignment to RaidAllyBufferRunCommand"
      pattern: "gBattlerControllerFuncs.*RaidAllyBufferRunCommand"
    - from: "sRaidAllyBufferCommands"
      to: "CONTROLLER_OPENBAG handler"
      via: "BtlController_Empty"
      pattern: "BtlController_Empty"
---

<objective>
Create src/battle_controller_raid_ally.c as a symbol-renamed clone of battle_controller_player_partner.c, and add SetControllerToRaidAlly to include/battle_controllers.h.

Purpose: Satisfies BATTLE-02 — CPU allies execute AI-chosen moves with no Bag, Run, or Poké Ball options available.
Output: src/battle_controller_raid_ally.c (new file); void SetControllerToRaidAlly(u32 battler) declaration in include/battle_controllers.h.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-battle-core/03-RESEARCH.md

@src/battle_controller_player_partner.c
@include/battle_controllers.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/battle_controller_raid_ally.c</name>
  <files>src/battle_controller_raid_ally.c</files>
  <action>
Create src/battle_controller_raid_ally.c as a full copy of src/battle_controller_player_partner.c. Apply the following symbol renames throughout (every occurrence):

| Old symbol | New symbol |
|---|---|
| `PlayerPartner` | `RaidAlly` |
| `sPlayerPartnerBufferCommands` | `sRaidAllyBufferCommands` |
| `SetControllerToPlayerPartner` | `SetControllerToRaidAlly` |
| `PlayerPartnerBufferExecCompleted` | `RaidAllyBufferExecCompleted` |
| `PlayerPartnerBufferRunCommand` | `RaidAllyBufferRunCommand` |

Any function named `PlayerPartnerHandle*` becomes `RaidAllyHandle*` (same suffix).

Keep all includes identical to the original file. Do NOT alter any behavior: CONTROLLER_OPENBAG stays wired to BtlController_Empty; CONTROLLER_CHOOSEACTION stays wired to the AI selection handler; all other dispatch entries are unchanged.

Verify: no `PlayerPartner` symbol remains in the new file.
  </action>
  <verify>`make -j4` succeeds. No duplicate symbol linker errors. If "undefined reference to SetControllerToRaidAlly" appears in a later plan, check whether the Makefile discovers src/*.c via wildcard — if it uses an explicit list, add `src/battle_controller_raid_ally.c` to the appropriate OBJ or SRC variable in the Makefile.</verify>
  <done>src/battle_controller_raid_ally.c exists; SetControllerToRaidAlly is defined; all identifiers use the RaidAlly prefix; no PlayerPartner identifiers remain in the new file; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Declare SetControllerToRaidAlly in include/battle_controllers.h</name>
  <files>include/battle_controllers.h</files>
  <action>
In include/battle_controllers.h, locate the existing declaration for SetControllerToPlayerPartner. Add the following line immediately after it, matching the surrounding formatting style:

```c
void SetControllerToRaidAlly(u32 battler);
```
  </action>
  <verify>`make -j4` succeeds. src/battle_controllers.c (which calls SetControllerToRaidAlly in Plan 03-01) must compile without undeclared-identifier errors.</verify>
  <done>include/battle_controllers.h contains `void SetControllerToRaidAlly(u32 battler);`.</done>
</task>

</tasks>

<verification>
`make -j4` completes without errors or new warnings. No duplicate symbol or undeclared identifier issues related to RaidAlly.
</verification>

<success_criteria>
- src/battle_controller_raid_ally.c exists as a complete renamed clone of battle_controller_player_partner.c
- All symbols in the new file use the RaidAlly prefix; zero PlayerPartner symbols remain
- include/battle_controllers.h declares void SetControllerToRaidAlly(u32 battler)
- Full build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-battle-core/03-02-SUMMARY.md`
</output>
